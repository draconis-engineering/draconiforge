[project]
name = "{{.ProjectName}}"
version = "0.1.0"
description = "Scaffolded with draconiforge"
authors = [{name="{{.Author}}"}]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
